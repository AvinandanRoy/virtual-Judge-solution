#include<stdio.h>
int main()
{
    int testCase , num1 , num2 ,i,renum1 , remum2,sum, resum;

    while(scanf("%d",&testCase) == 1 ){
        for(i = 1 ; i <= testCase ; i++){
            scanf("%d%d",&num1 ,&num2 );
        }
    }
}
